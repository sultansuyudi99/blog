<% content_for :title, "Sign up" %>

<h1 class="text-6xl font-bold text-center">Sign Up</h1>

<%= render "form", user: @user %>

<p class="text-neutral-500 text-center">Already have an accout? <%= link_to "Login", signup_path, class: "underline hover:text-blue-500 transition-colors" %></p>
